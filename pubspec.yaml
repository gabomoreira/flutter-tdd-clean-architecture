name: manguinho
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: 
  mockito: ^5.4.2
  build_runner: ^2.4.6
  faker: 

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

